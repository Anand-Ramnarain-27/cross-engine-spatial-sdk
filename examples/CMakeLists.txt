# Example / demo applications for the Cross-Engine Spatial Data Streaming SDK.
#
# StandaloneViewer (Phase 9) is added first, since it is the primary
# development and debugging environment for the SDK. UnityDemo, UnrealDemo,
# and CustomEngineDemo follow in Phases 10-12 and are built through their
# respective engine toolchains rather than this CMake tree.
