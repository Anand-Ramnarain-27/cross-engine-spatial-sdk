# Public SDK API

Written once the `API` layer (`SpatialWorld`, `SpatialDataset`, `Camera`, `StreamingManager` facade) is implemented — see the development order in the project brief. Not yet implemented (current phase: 1, Project Foundation).
